# opt

`opt` is the full nonlinear optimization crate from this repository.

It includes:

- `Bfgs`
- `NewtonTrustRegion`
- `Arc`
- automatic solver selection through `Problem`, `SecondOrderProblem`, and `optimize`

Add it with:

```toml
[dependencies]
opt = "0.1.0"
```
